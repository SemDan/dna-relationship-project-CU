# Проект: Анализ родства организмов по ДНК

Этот проект посвящён анализу генетических последовательностей различных организмов и оценке их эволюционной близости на основе сходства ДНК.

Основная идея проекта — сравнить гены разных организмов и построить **таблицу родственности**, отражающую степень их сходства.

Проект реализован на Python и организован как модульный пайплайн.

---

# Структура проекта

    dna-relationship-project/
    ├── Data/
    │   └── Base/               # Входные данные (.fna файлы с ДНК)
    ├── notebooks/
    │   └── stage1.ipynb        # Ноутбук с демонстрацией результатов этапа 1
    ├── src/
    │   ├── io_utils.py         # Чтение и обработка .fna файлов
    │   ├── similarity.py       # Алгоритм вычисления сходства (LCS)
    │   └── ranking.py          # Построение таблицы родственности
    ├── results/                # Сгенерированные результаты
    ├── requirements.txt        # Зависимости проекта
    └── README.md

---

# Этап 1 — Чтение ДНК и анализ сходства

Первый этап проекта включает:

1. Чтение ДНК последовательностей из `.fna` файлов
2. Извлечение названия организма из заголовков FASTA
3. Построение одной репрезентативной последовательности для каждого организма
4. Сравнение организмов с помощью **наибольшей общей подпоследовательности (LCS)**
5. Построение **матрицы родственности организмов**

Основная гипотеза:

> Чем длиннее общая подпоследовательность ДНК у двух организмов, тем ближе их эволюционное родство.

---

# Входные данные

Используется **10 генетических последовательностей реальных организмов**.

Примеры видов:

- Homo sapiens
- Mus musculus
- Bos taurus
- Canis lupus familiaris
- Felis catus
- Equus caballus
- Mesocricetus auratus
- Danio rerio
- Drosophila melanogaster
- Caenorhabditis elegans

Каждый организм хранится в отдельном `.fna` файле.

Файлы имеют формат **FASTA**.

Пример записи:

    >NC_037345.1 RPL18 [organism=Bos taurus]
    ATGCGATCGATCGATCGATCG...

---

# Стратегия обработки файлов

Некоторые `.fna` файлы могут содержать **несколько FASTA-записей**.

Чтобы получить одну последовательность для организма, используется следующая стратегия:

1. Считываются **все FASTA-записи**
2. Из заголовка извлекается `[organism=...]`
3. В качестве представителя организма выбирается **самая длинная последовательность**

Это позволяет корректно сравнивать организмы между собой.

---

# Метрика сходства

Для оценки сходства используется длина наибольшей общей подпоследовательности:

    LCS(seq1, seq2)

Чтобы учесть разную длину последовательностей, используется нормированная метрика:

    similarity = LCS(seq1, seq2) / min(len(seq1), len(seq2))

Интерпретация значений:

- `0` — последовательности сильно отличаются
- `1` — последовательности идентичны

---

# Результат

Программа строит **матрицу сходства организмов**.

Пример:

| Species | Human | Mouse | Dog |
|---|---:|---:|---:|
| Human | 1.00 | 0.83 | 0.79 |
| Mouse | 0.83 | 1.00 | 0.81 |
| Dog | 0.79 | 0.81 | 1.00 |

После этого формируется **таблица родственности**, где:

- `1` — максимально близкие организмы
- `10` — максимально далекие

---

# Установка проекта

Создать виртуальную среду:

    python3.11 -m venv .venv
    source .venv/bin/activate

Установить зависимости:

    pip install -r requirements.txt

---

# Запуск проекта

Пример использования:

    from src.io_utils import load_sequences_from_folder
    sequences = load_sequences_from_folder("Data/Base")

Также можно запустить ноутбук:

    jupyter notebook notebooks/stage1.ipynb

---

# Используемые библиотеки

Основные зависимости проекта:

- Python 3.11
- pandas
- matplotlib
- seaborn
- jupyter

Все зависимости перечислены в файле `requirements.txt`.

---

# Дальнейшее развитие проекта

Следующие этапы проекта могут включать:

- построение филогенетического дерева
- оптимизацию алгоритмов сравнения последовательностей
- улучшение метрик сходства
- визуализацию эволюционных связей

---

# Автор

Danila Semenov